<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.onlinechat.module.friend.mapper.FriendGroupMapper">

    <resultMap id="FriendGroupMap" type="cn.edu.ncu.onlinechat.module.friend.entity.FriendGroup">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="name" property="name" />
        <result column="sort_order" property="sortOrder" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="selectById" resultMap="FriendGroupMap">
        SELECT *
        FROM t_friend_group
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="FriendGroupMap">
        SELECT *
        FROM t_friend_group
        WHERE user_id = #{userId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_friend_group
        (user_id, name, sort_order)
        VALUES
        (#{userId}, #{name}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE t_friend_group
        SET name = #{name},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_friend_group
        WHERE id = #{id}
    </delete>

</mapper>
